//TODO: WORK IN PROGRESS

// import type { ZodObject, safeParse, safeParseAsync } from "zod";

// type Payload <TBody = Record<string, unknown> , TParams = Record<string, string | string[]>, TQuery = Record<string, string | string[]>> ={
//   body:TBody,
//   params:  Promise<TParams>,
//   query: TQuery
// }

// export const RequestValidator = <B, P, Q, S = ZodObject>(payload: Payload<B, P, Q>,  schema: S) => {

// }
