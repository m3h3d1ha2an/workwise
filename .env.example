# ==========================================
# Example .env.example for createEnv (t3-oss/env-nextjs)
# - Fill values in production and DO NOT commit real secrets.
# - Your createEnv server schema expects exact secret lengths (see comments).
# ==========================================


# PostgreSQL database connection URL
# Format example:
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=


# Node environment
# Allowed: development, test, production
# Default used by schema: development
NODE_ENV=development


# --------------------------------------------------
# ACCESS TOKEN
# --------------------------------------------------
# Purpose:
# - Short-lived JWT used for authenticating API requests.
#
# How to generate (OS-agnostic using Node.js; yields 64 hex chars = 32 bytes):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Schema requirement:
# - Must be hex and length 64 (32 bytes).
#   Your Zod: z.hex().length(64)
#
# Common best practice:
# - ACCESS_TOKEN_EXPIRES_IN examples: 5m, 10m, 15m, 30m
# - Use short lifetimes (5m–30m) and rotate secrets if compromised.
ACCESS_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRES_IN=15m


# --------------------------------------------------
# REFRESH TOKEN
# --------------------------------------------------
# Purpose:
# - Longer-lived token used to obtain new access tokens.
#
# How to generate (OS-agnostic using Node.js; yields 128 hex chars = 64 bytes):
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# Schema requirement:
# - Must be hex and length 128 (64 bytes).
#   Your Zod: z.hex().length(128)
#
# Common best practice:
# - REFRESH_TOKEN_EXPIRES_IN examples: 3d, 7d, 14d, 30d
# - Store refresh tokens securely (httpOnly cookies or secure storage).
REFRESH_TOKEN_SECRET=
REFRESH_TOKEN_EXPIRES_IN=7d


# --------------------------------------------------
# Optional: Skip validation when building (useful in CI/Docker)
# If you need to skip env validation during build, set:
#   SKIP_ENV_VALIDATION=true
# This matches: skipValidation: !!process.env.SKIP_ENV_VALIDATION
SKIP_ENV_VALIDATION=
